// Canonical scout OG image: gittinder.com/<username> links unfurl with the
// profile's match card. The implementation lives in /u/<username>; this reuses
// it so both routes render the same image.
export { default } from "../u/[username]/opengraph-image";
